# Pico Computer 3 emulator (see ports/rp2/boards/PICO_COMPUTER_3/emulator/).
# Phase 1: terminal console -- the interpreter + frozen board modules + shims.

FROZEN_MANIFEST ?= $(VARIANT_DIR)/manifest.py
